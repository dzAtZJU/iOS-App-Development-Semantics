extension Swift {
    enum Expressive {
        case Opaquereturntype
        case Propertywrapper
        case DSL
    }
    
    enum Painpoint {
        case Boilerplate
        case MoreThanOnePlacesNeedModify
    }
    
    enum Protocol {
        case ConditionalConformanceSynthesize
        case Generics
    }
}
