enum Optimize {
    enum Strategy {
        case Avoid_Unnecessary
        case Minimize_Work
        case Cache_Work
        case Share_Work
        case Combine_Work
        case Pool
        case Keepalive
        case Leveraging
        case Defer
        case Businesscontrol
    }
}
