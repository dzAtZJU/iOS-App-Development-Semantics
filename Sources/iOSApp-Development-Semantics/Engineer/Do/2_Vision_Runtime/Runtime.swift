enum Runtime {
    enum Condition {
        case Performance
        case BatteryLife
        
        enum Performance {
            enum Effects {
                case Memory
            }
        }
    }
}
