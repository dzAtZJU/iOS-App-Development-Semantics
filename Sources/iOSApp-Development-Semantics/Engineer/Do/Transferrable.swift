// MindSet Breakthrough:
    // idea -> action: create + apply + learn -> time -> learning cost -> transferrable

// Market + Competition -> Comparing + Choice
enum Market {
    struct StackshareIO: Resource {}
    struct Appsight: Resource {}
    struct Cocoapods: Resource {}
    struct Libhunt: Resource {}
    struct IOSDevSurvey: Resource {}
}

// frustration
    // Obstacle
        // google search doesn't handle "scene solution search"
            // overcome: study relevant field
        // explore semantics posibility -> gradually get familar with field semantics
            // google
            // discussions on stackoverflow
            // study semantics
        // Math & Analyze: 极其精细，对于概念要非常清晰
    // belief in the world
