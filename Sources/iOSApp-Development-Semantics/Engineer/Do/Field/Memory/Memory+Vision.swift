extension Memory {
    enum Vision {
        enum Standards {
            case Never_enough_to_go_around
        }
        
        enum Rationalize {
            case Strings
            case Images
            case CoreDataManagedObjects
            case Mental_Model_of_accessed_resources = "group by user interaction"
        }
    }
}
