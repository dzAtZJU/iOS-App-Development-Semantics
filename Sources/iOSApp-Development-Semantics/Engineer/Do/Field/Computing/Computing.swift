enum Computing {
    enum Task {
        enum Operation {
            case Cancel
        }
        
        enum Concurrency {
            enum Tuning {
                case SystemDeciding
            }
        }
        
        enum Dependencies {
            
        }
    }
    
    enum Technology {
        enum NSOperationQueue {
            enum NSOperation {
                enum StateMachine {
                    case Pending
                    case Ready
                    case Executing
                    case Finished
                    case Cancelled
                }
            }
        }
    }
}
