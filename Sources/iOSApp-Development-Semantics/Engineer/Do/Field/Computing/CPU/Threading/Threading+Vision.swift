extension Threading {
    enum Vision {
        enum Technology {
            enum GCD {
                enum Runtime {
                    case Dispatch_Task_to_Thread
                }
            }
        }
    }
}
