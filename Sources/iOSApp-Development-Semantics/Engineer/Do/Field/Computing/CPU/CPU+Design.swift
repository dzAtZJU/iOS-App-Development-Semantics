extension CPU {
    enum Design {
        case Concurrency = ["Concurrency1.png", "Concurrency2.png"]
        
        enum QoS {
            enum Background {
                enum Scheduling {
                    // wwdc 2014 energy efficient
                }
            }
        }
    }
}
