enum Layout {
    enum Technology {
        enum AutoLayout {
            enum AlgoComplexity {
                case InDependentSiblingViews = "Linearly"
                case DependentSiblingViews = "Linearly"
                case NestedViews = "Linearly"
            }
        }
    }
}

