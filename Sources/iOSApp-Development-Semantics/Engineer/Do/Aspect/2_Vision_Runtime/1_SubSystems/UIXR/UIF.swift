enum UIF {
    enum Task {
        case Creation
        case Animation
        
        enum Layout {
            enum Task {
                case MeasureTexts
            }
            enum Technology {
                enum Autolayout {
                    enum Design {
                        case Reduce_Constraints
                    }
                }
            }
        }
        
        enum Draw {
            case DrawTexts
        }
    }
}
