enum Leveraging {
    enum Image {
        enum Format {
            case HEIC
        }
    }

    enum Assetcatalog {
        
    }

    enum Coredata {
        
    }
    
    enum Buildsettings {}
    
    enum SIMD {}
    
    enum URLSession {}
    enum Network {
        case OptimisticDNS
    }
    // TLS1.3 -> 1 round trip
    
    enum NSCache {}
}

