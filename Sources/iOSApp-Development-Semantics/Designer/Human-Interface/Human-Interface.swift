enum HumanInterface {
    enum Items {
        case Arrangement
        case Separate
        case Hidden
    }
    
    enum Item {
        enum Transition {
            
        }
        
        enum Layout {
            case Items
        }
        
        enum Interfere {
            case Near
            case Dim
        }
    }
}
